- #dev
	- Cài starship theming cho đẹp
		- https://starship.rs/
		- `curl -sS https://starship.rs/install.sh | sh -s -- -y && mkdir -p ~/.config && starship preset tokyo-night -o ~/.config/starship.toml && echo 'eval "$(starship init bash)"' >> ~/.bashrc && source ~/.bashrc`
	- Xài thử wezterm - terminal emulator - thay cho Terminal của MacOS
	- Cài macos 15 sequoia
-